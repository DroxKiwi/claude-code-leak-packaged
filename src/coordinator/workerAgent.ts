import type { AgentDefinition } from '../tools/AgentTool/loadAgentsDir.js'

/** Stub coordinator : le module complet n’est pas présent dans ce fork. */
export function getCoordinatorAgents(): AgentDefinition[] {
	return []
}
